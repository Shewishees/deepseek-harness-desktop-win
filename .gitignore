# Release binaries are uploaded as GitHub Release assets, never committed to Git.
release/*.zip
release/*.dmg
release/*.app/
release/*.exe

# macOS metadata
.DS_Store

# Local build and tooling artifacts
source/
.tools/
.unpacked_patch/
unpacked/
*.log
