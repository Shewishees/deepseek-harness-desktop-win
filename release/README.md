# Release assets

Published GitHub Releases contain the signed `DeepSeek-Harness-mac-arm64.zip` installer and its SHA-256 checksum.

No local data, photos, conversations, workspace files, credentials, caches, build directories, or source `node_modules` are included in this repository or the release asset.
